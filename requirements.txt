# Async Expert Incubator - Dependencies
# Python 3.10+

# Web Search (Modernized)
duckduckgo-search>=8.1.1

# HTML Parsing and Content Extraction (Updated)
trafilatura==2.0.0

# HTTP Client (sync, used by orchestrator for Wikidata API calls)
requests>=2.31.0

# Async HTTP client
aiohttp>=3.10.0
httpx>=0.27.0

# Phase A: Wikidata Streaming (Zero-RAM-Bloat)
ijson>=3.3.0

# Database Connection Pooling
# (Using built-in sqlite3, no additional dependencies needed)

# Reports (matplotlib for EMA evolution charts)
matplotlib>=3.8.0

# API
fastapi>=0.110.0
uvicorn>=0.27.0

# Configuration
pydantic-settings>=2.1.0

# VRAM watchdog (optional)
pynvml>=13.0.0

# Testing
pytest>=9.0.0

# Type Hinting (included in Python 3.10+, but listed for clarity)
# typing-extensions>=4.0.0
