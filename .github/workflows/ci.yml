name: CI
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest ruff
      - name: Environment info
        run: |
          python --version
          pip list
          python -c "import sys; print('Python ok')"
      - name: Lint
        run: ruff check . --ignore=E501,E402,F403,F405,F811 2>&1 || true
      - name: Test imports
        run: |
          set -o pipefail
          python tests/test_orchestrator_imports.py 2>&1 | tee /tmp/ci-test-output.log
        shell: bash
        timeout-minutes: 5
      - name: Show test output in summary
        if: failure()
        run: |
          echo "### Test output" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          cat /tmp/ci-test-output.log >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
      - name: Run unit tests
        run: python -m pytest tests/ -v --ignore=tests/test_web_scraper.py --ignore=tests/test_dissect_wikidata_deep.py --ignore=tests/test_dissect_wikidata_imports.py --ignore=tests/test_orchestrator_phase_b.py 2>&1
        timeout-minutes: 5
